<?php

/**
 * @file
 * Code for the Hedley FAQ feature.
 */

include_once 'hedley_faq.features.inc';

/**
 * Implements hook_ctools_plugin_directory().
 */
function hedley_faq_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools') {
    return 'plugins/' . $plugin;
  }
}
